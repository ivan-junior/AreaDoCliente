export interface IOpenCase  {
    contact: string | null,
    description: string | null,
    email: string | null,
    name: string | null,
    phone: string | null,
    projectNumber: string | null,
    reason: string | null
}